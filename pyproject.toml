[project]
name = "fastapi-project"
version = "0.1.0"
description = "FastAPI professional template"
requires-python = ">=3.12"

dependencies = [
    "fastapi",
    "uvicorn",
    "sqlalchemy>=2.0.0",
    "pydantic-settings",
]


[tool.uv]
package = false

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
